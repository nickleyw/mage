services:
  xmage-web-bridge:
    build:
      context: ..
      dockerfile: Mage.WebBridge/Dockerfile
    environment:
      XMAGE_BRIDGE_TOKEN: ${XMAGE_BRIDGE_TOKEN:?Set XMAGE_BRIDGE_TOKEN first}
      JAVA_OPTS: -Xms256m -Xmx1536m
    ports:
      - "8080:8080"
    restart: unless-stopped
